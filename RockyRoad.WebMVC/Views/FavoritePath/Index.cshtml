@model IEnumerable<RockyRoad.Models.FavoritePathListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PathId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FavID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) 
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PathId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FavID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FavID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FavID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FavID })
        </td>
    </tr>
}

</table>
